def brute_force(points):
   for p0 in points:
      for p1 in points:
         for p2 in points:
            for p3 in points:
               check_points(p0, p1, p2, p3)

def slope_sort(points, n):
   pass
   # for p0 in points:
   #    